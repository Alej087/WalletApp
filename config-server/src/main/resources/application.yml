
server:
  port: 8889

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alej087/WalletApp
          default-label: main
          force-pull: true
          search-paths: configData